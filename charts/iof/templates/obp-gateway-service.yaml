{{- if .Values.obpGateway.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "iof.fullname" . }}-obp-gateway
  labels:
    {{- include "iof.componentLabels" (dict "component" "obp-gateway" "root" .) | nindent 4 }}
spec:
  type: {{ .Values.obpGateway.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.obpGateway.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iof.componentSelectorLabels" (dict "component" "obp-gateway" "root" .) | nindent 4 }}
{{- end }}
