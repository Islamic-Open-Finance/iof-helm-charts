{{- if .Values.finopsApi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "iof.fullname" . }}-finops-api
  labels:
    {{- include "iof.componentLabels" (dict "component" "finops-api" "root" .) | nindent 4 }}
spec:
  type: {{ .Values.finopsApi.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.finopsApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iof.componentSelectorLabels" (dict "component" "finops-api" "root" .) | nindent 4 }}
{{- end }}
