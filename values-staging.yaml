global:
  environment: staging

apiGateway:
  replicaCount: 2
  autoscaling:
    minReplicas: 2
    maxReplicas: 10

postgresql:
  readReplicas:
    replicaCount: 1

redis:
  replica:
    replicaCount: 1
